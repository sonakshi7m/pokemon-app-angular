import { EffortValuePipe } from './effort-value.pipe';

describe('EffortValuePipe', () => {
  it('create an instance', () => {
    const pipe = new EffortValuePipe();
    expect(pipe).toBeTruthy();
  });
});
